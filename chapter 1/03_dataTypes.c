#include <stdio.h>

int main()
{
    int a = 1;
    float b = 1.4;
    // Character should be in single quote
    char c = 'c';

    printf("the value of a is %d", a);
    printf("\nthe value of b is %f", b);
    printf("\nthe value of c is %c", c);
    return 0;
}