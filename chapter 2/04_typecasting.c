#include <stdio.h>

int main(){
    float a =10.89;
    int n;
    // Convert the float into int
    n = (int)a;
    printf("%d\n", n);

    float l = 6;
    printf("l is %f\n", l);

    int b = 10.3;
    printf("b is %d", b);
    return 0;
}